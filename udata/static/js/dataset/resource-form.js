/**
 * Dataset form
 */
define(['logger', 'form/common', 'form/markdown', 'form/format-completer'], function(log) {
    return {
        start: function() {
            log.debug('Resource form loaded');
        }
    }
});
