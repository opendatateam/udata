/**
 * Reuse form
 */
define(['logger', 'form/common', 'form/markdown', 'form/tag-completer', 'form/dataset-completer'], function(log) {
    return {
        start: function() {
            log.debug('Reuse form page');
        }
    }
});
